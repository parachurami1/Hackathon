version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      # Environment variables can be used if needed
      DB_FILE: /var/www/html/site/database.sqlite
